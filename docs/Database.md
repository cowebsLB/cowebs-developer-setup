# Database

This project has no application database or schema.

The Backend profile can install PostgreSQL 18 as a developer tool. Database initialization, credentials, ports, services, and project schemas are intentionally left to the user and are never stored by the setup script.
