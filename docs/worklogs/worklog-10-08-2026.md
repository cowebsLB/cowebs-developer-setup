# Worklog - 10-08-2026

## Summary

Completed the next Ubuntu Phase 2 catalog slice by classifying 14 database, API-client, browser, networking, design, and Android tools. Added nine executable typed providers, five explicit unsupported results, four digest-pinned APT repository prerequisites, deterministic planner coverage, and current user/contributor documentation. The repository now has 40 reviewed Ubuntu classifications: 25 executable providers, 15 explicit unsupported results, and 46 packages remaining.

## Why

The Linux roadmap requires every shared logical package to retain its product intent while gaining an exact Ubuntu provider or a clear fail-closed reason. This slice advances that work without introducing arbitrary shell commands, mutable bootstrap sources, unverified downloaded-package execution, interactive repository automation, or unsupported third-party desktop wrappers.

## How

Reviewed current primary vendor, Ubuntu, Snap Store, and Flathub evidence for each package and checked live repository architecture metadata. Added only provider paths representable by the existing typed APT, Snap, and Flatpak contracts. Downloaded the current official repository-key bytes in memory, calculated their SHA-256 digests, and stored those digests in reusable prerequisites. Extended compiler and planner regression tests for exact mappings, privilege/scope, architecture constraints, prerequisite deduplication, deterministic output, and complete unsupported diagnostics. No software installation or repository mutation was performed.

Primary evidence reviewed included PostgreSQL's Ubuntu repository documentation, DBeaver installation documentation, Bruno's Linux download documentation, Postman's Linux installation documentation, Redis Insight desktop documentation, MongoDB Compass installation documentation, MySQL Workbench and MySQL APT documentation, Google's Linux repository key documentation, Mozilla's Linux installation documentation, Figma's desktop/system requirements, Cloudflare Tunnel installation documentation, ngrok's Linux download documentation, Android Studio's Linux installation documentation, Ubuntu Noble package metadata for scrcpy, live Snap Store metadata, and live repository package indexes.

## When

Codex Linux catalog session completed on 2026-08-10 at 10:11 +03:00 (Asia/Beirut).

## Where

- `config/packages.json`
- `tests/test-architecture-foundation.ps1`
- `tests/test-ubuntu-planning.ps1`
- `README.md`
- `CHANGELOG.md`
- `docs/roadmap.md`
- `docs/features.md`
- `docs/architecture.md`
- `docs/installation.md`
- `docs/Testing.md`
- `docs/Security.md`
- `docs/worklogs/worklog-10-08-2026.md`

The unrelated pre-existing `.gitignore` modification was preserved and remains outside this task.

## Changes Made

- Added conditional PostgreSQL 18 APT support for x64 and arm64 through the official PGDG Noble repository.
- Added conditional user-scoped Bruno support through the vendor-documented Flathub application ID on x64.
- Added conditional Postman Snap support for x64 and arm64 using the verified publisher entry.
- Added conditional Redis Insight Snap support on x64 using the installation path documented by Redis.
- Added conditional Google Chrome APT support for x64 and arm64 from Google's current repository metadata.
- Added conditional Mozilla Firefox Snap support for x64 and arm64 using Mozilla's verified publisher entry.
- Added conditional Cloudflare Tunnel and ngrok APT support for x64 and arm64.
- Added native Ubuntu Noble scrcpy support for x64 and arm64.
- Added explicit unsupported classifications for DBeaver, MongoDB Compass, MySQL Workbench, Figma desktop, and Android Studio because their official paths currently require local artifacts, interactive setup, or do not include a native Linux desktop application.
- Added four typed signed-repository prerequisites. Current pinned SHA-256 values are PostgreSQL `0144068502a1eddd2a0280ede10ef607d1ec592ce819940991203941564e8e76`, Google `54dea5f6c2a26091578cf52a999cebc6b64df478d37ad4dce96376b711e3b27c`, Cloudflare `1bd95f4082b320d541bee351560fc2765aa9f9cd8efa4c9e32135e63f252721d`, and ngrok `8a57c28e1779e2a8e5bba3865fffd6805e15898988c235eae862f3069c3f2c28`.
- Extended architecture tests to require 40 classifications, 25 providers, 15 explicit unsupported results, five signed prerequisites, exact key digests, manager/package IDs, architecture constraints, and Bruno's user/user Flathub scope.
- Extended planner tests with deterministic supported and unsupported productivity/tooling profiles, exact install order, repository operation counts, one shared APT refresh, and complete fail-closed diagnostics.
- Updated roadmap totals and reduced the remaining Ubuntu classification count from 60 to 46.

## Problems Encountered

- The first planner-test invocation selected the known incomplete ignored `.tmp/go-toolchain` extraction and failed because its Go standard library lacked `src/weak`.
- Several official Linux products expose only direct DEB/archive downloads or interactive repository configuration, which the current safe provider schema intentionally cannot execute.
- Repository signing keys are controlled by external vendors and can rotate after the catalog digest is recorded.

## Solutions

- Selected the existing complete Go 1.26.5 extraction via the documented `COWEBS_GO_EXE` override and reran the same test unchanged; it passed.
- Kept unrepresentable provider paths explicit and unsupported instead of adding arbitrary shell, local-artifact, consent, or interactive-configuration fields.
- Calculated each repository-key digest from the live official HTTPS resource and retained fail-closed digest verification; future legitimate key rotation requires a reviewed catalog update.

## Bugs Found

- No product-code regression was found in the existing compiler, planner, or Linux adapter.
- The local default fallback Go cache remains incomplete and cannot build the current module without the complete-toolchain override.
- Existing documentation totals became stale as soon as the new classifications were added.

## Bug Fixes

- Updated every affected test and documentation total to the new 40-classification, 25-provider, 15-unsupported, five-prerequisite baseline.
- Documented the local toolchain-cache limitation and used the verified complete extraction for all authoritative validation.
- No ignored cache content was committed or treated as repository source.

## Testing

- Unit Tests: `go test -count=1 -cover ./...` passed. Statement coverage was Linux adapter 80.6%, Windows adapter 46.8%, broker 66.7%, catalog 66.9%, doctor 64.7%, journal 66.7%, and planner 87.4%; the CLI wrapper is covered by black-box integration and reports 0.0% in-process coverage.
- Integration Tests: `tests/test-architecture-foundation.ps1` and `tests/test-ubuntu-planning.ps1` passed independently. The complete suite also passed schema-v3 compilation, all new provider/prerequisite assertions, the nine-provider deterministic plan, five unsupported diagnostics, 19 successful PowerShell/Go parity scenarios, expected conflict rejection, broker checks, journal/resume/status, and doctor tests.
- End-to-End Tests: `tests/run-tests.ps1` passed. It covered all Windows profile default/essentials dry-runs, manifest and documentation contracts, bootstrap CRLF and handoff behavior, release ZIP creation, checksum verification, extraction, dry-run execution, and cleanup.
- Manual Testing: Confirmed the live branch and remote baseline matched before edits; queried current primary-source installation paths, Snap publishers and stable architectures, Flathub application metadata, APT repository package indexes, and official key bytes; confirmed no real package install or repository write occurred; preserved the unrelated `.gitignore` edit.
- Static Checks: `go vet ./...` passed. JSON parsing and deterministic schema-v2 to schema-v3 compilation passed before the full suite. Final `git diff --check`, scoped secret-pattern scanning, staging review, and remote-divergence checks are performed in the production-ship pass.
- Pass/Fail Status: The initial planner invocation failed only because it selected the incomplete ignored Go cache. The same test and every authoritative gate passed with the complete declared Go toolchain.
- Edge Cases Tested: x64-only Bruno and Redis Insight constraints, user-scoped Flathub source retention, exact signed-repository digests, five repositories sharing one APT refresh, deterministic repeated JSON, unsupported packages emitted together and in logical order, unknown prerequisite references, credential-bearing URLs, arbitrary shell fields, and malformed typed provider data.

## Performance Notes

The combined uncached coverage, vet, and complete repository suite finished in approximately 181 seconds. The new planner slice adds four repository key/source pairs but still emits only one APT package-index refresh. No runtime performance regression was observed; the production Windows runtime is unchanged.

## Advanced Warnings

- Schema v2 and `src/windows/setup.ps1` remain the production runtime. These Linux providers are source-only planning inputs and are not included in the public v6.2 runtime ZIP.
- Real Linux prerequisite mutation remains deliberately disabled until Phase 4 implements verified downloads, atomic file replacement, permissions, and one typed elevated controller handoff.
- No supported Linux installation command exists yet. The planned public surface remains `cowebs install dev-setup` after controller, packaging, bootstrap, and disposable-environment gates are complete.
- DBeaver, MongoDB Compass, MySQL Workbench, Figma desktop, and Android Studio are not silently omitted; selected plans fail with explicit unsupported diagnostics.
- ngrok authentication, Cloudflare login/tunnel setup, PostgreSQL service policy, and desktop application first-run/account state remain separate future configuration intents.
- Bruno and Redis Insight are x64-only in this reviewed slice. Provider architecture availability must be revalidated before expanding them.
- Vendor repository keys and package architecture indexes can change. Digest or availability changes must fail closed and receive a reviewed catalog update.
- Real installation testing belongs in a disposable Ubuntu 24.04 VM or container with appropriate isolation, never on the active developer workstation.
- The ignored `.tmp/go-toolchain` extraction remains incomplete; local validation should use the complete Go 1.26.5 extraction through `COWEBS_GO_EXE` or replace the cache outside repository history.
- This task authorizes a source commit and non-force push only. It does not authorize a Git tag, GitHub release, release asset, or deployment.

---

# Kubernetes, IaC, Automation, and Secrets Slice

## Summary

Classified the next 18 ordered Ubuntu packages covering Windows-host intent, Kubernetes tooling, Infrastructure as Code, automation, secrets, and encryption. Added eleven executable providers and seven explicit unsupported results. The Ubuntu catalog now contains 58 reviewed classifications: 36 executable providers, 22 unsupported results, seven signed APT prerequisites, and 28 packages remaining.

## Why

This roadmap slice contains tightly related Kubernetes and infrastructure tools and allows Terraform, Vault, and Packer to share one reviewed HashiCorp repository prerequisite. Classifying the two WSL entries at the same time prevents Windows-only logical intent from being silently translated into meaningless Ubuntu operations.

## How

Reviewed primary installation documentation and live package metadata for every package. Queried current Snap Store publisher, validation, stable-channel architecture, and confinement metadata for kubectl, Helm, yq, OpenTofu, and Task. Verified Ubuntu Noble package availability for kubectx and age. Downloaded the official Trivy and HashiCorp signing-key bytes in memory, calculated SHA-256 digests, and checked amd64/arm64 repository indexes. Added only typed APT and Snap providers representable by the current schema; release binaries, archive downloads, shell installers, Homebrew-only paths, and Windows-host features remain fail-closed.

## When

Continuation session on 2026-08-10 at 10:27 +03:00 (Asia/Beirut).

## Where

- `config/packages.json`
- `tests/test-architecture-foundation.ps1`
- `tests/test-ubuntu-planning.ps1`
- `README.md`
- `CHANGELOG.md`
- `docs/roadmap.md`
- `docs/features.md`
- `docs/architecture.md`
- `docs/installation.md`
- `docs/Testing.md`
- `docs/Security.md`
- `docs/worklogs/worklog-10-08-2026.md`

The unrelated `.gitignore` modification remains outside the task.

## Changes Made

- Classified WSL and Ubuntu for WSL as unsupported Windows-host intent on an Ubuntu target.
- Added conditional classic Snap providers for kubectl, Helm, OpenTofu, and Task on x64 and arm64.
- Added the upstream-documented strict yq Snap for x64 and arm64, preserving the Mike Farah product identity instead of selecting Ubuntu's unrelated Python `yq` implementation.
- Added native Ubuntu providers for kubectx and age on x64 and arm64.
- Added Trivy through its official signed APT repository on x64 and arm64.
- Added Terraform, Vault, and Packer through one shared official HashiCorp APT prerequisite on x64 and arm64.
- Classified K9s, kind, Flux, TFLint, and SOPS as unsupported until verified local-artifact or installer handling exists.
- Pinned the current Trivy key SHA-256 as `067f4782e5f2a736710c5256a9695c3ccb4731727a6118da8d8f532be97ecb39` and HashiCorp key SHA-256 as `cafb01beac341bf2a9ba89793e6dd2468110291adfbb6c62ed11a0cde6c09029`.
- Extended deterministic architecture and planner tests for 58 classifications, 36 providers, 22 unsupported results, seven prerequisites, exact classic Snap options, shared HashiCorp refresh dependencies, and dependency-aware kind/Docker diagnostics.
- Updated roadmap totals and reduced the remaining Ubuntu package count from 46 to 28.

## Problems Encountered

- Several package names have unsafe or misleading distribution alternatives: Ubuntu's `yq` is not the Mike Farah product requested by the logical catalog, and WSL packages have no valid Ubuntu-host installation meaning.
- OpenTofu's DEB repository uses two signing keys, which the current prerequisite model cannot safely represent as one source; its official verified classic Snap provides a typed alternative.
- kind depends on Docker Desktop in the shared catalog, which is already explicitly unsupported on Ubuntu.

## Solutions

- Selected only the upstream-documented Mike Farah Snap and added exact provider tests.
- Kept WSL intent explicit and unsupported rather than inventing a no-op or Ubuntu package substitute.
- Used OpenTofu's official verified classic Snap, avoiding a lossy multi-key repository representation.
- Made the unsupported planner test assert both kind and its Docker dependency so dependency intent remains visible.

## Bugs Found

- No compiler, planner, or Linux adapter defect was found during this slice.
- Existing documentation totals became stale after expanding the catalog.

## Bug Fixes

- Updated all affected documentation and test baselines to 58 classifications, 36 executable providers, 22 unsupported results, seven prerequisites, and 28 remaining packages.
- Added regression coverage preventing product-name substitution, classic-option loss, duplicate HashiCorp repository work, or omission of unsupported dependencies.

## Testing

- Unit Tests: `go test -count=1 -cover ./...` passed. Statement coverage was Linux adapter 80.6%, Windows adapter 46.8%, broker 66.7%, catalog 66.9%, doctor 64.7%, journal 66.7%, and planner 87.4%; the CLI wrapper remains black-box tested and reports 0.0% in-process coverage.
- Integration Tests: `tests/test-architecture-foundation.ps1` passed with exact provider, architecture, digest, confinement, and unsupported assertions. `tests/test-ubuntu-planning.ps1` passed with deterministic supported/unsupported infrastructure profiles.
- End-to-End Tests: The complete `tests/run-tests.ps1` suite passed after the documentation and worklog changes, including all Windows dry-runs, bootstrap contracts, release ZIP build, checksum verification, extraction, handoff, and cleanup.
- Manual Testing: Verified live Snap Store stable architectures and publishers, Ubuntu Noble package metadata, official repository package indexes, and official signing-key bytes. No package installation or repository mutation occurred.
- Pass/Fail Status: All affected tests passed on their first post-implementation run.
- Coverage Summary: Uncached coverage passed with planner at 87.4%, catalog at 66.9%, and Linux adapter at 80.6%; the expanded catalog paths are additionally exercised through PowerShell black-box integration.
- Edge Cases Tested: Windows-only host intent, WSL dependency ordering, Mike Farah yq identity, classic Snap options, shared HashiCorp prerequisite deduplication, single APT refresh, kind's unsupported Docker dependency, and complete deterministic unsupported ordering.

## Performance Notes

The new slice adds two repository key/source pairs, while one shared HashiCorp prerequisite serves three packages and the planner still emits only one APT refresh. The combined uncached coverage, vet, and complete repository gate finished in approximately 150 seconds. The production Windows runtime remains unchanged.

## Advanced Warnings

- All Linux providers remain source-only planning data; real prerequisite mutation and package installation are disabled.
- Helm's snap is maintained by Snapcrafters and is therefore conditional despite being documented by Helm. yq and Task publisher verification states are also captured by conservative conditional wording and must be revalidated before release.
- Vault installation does not initialize, unseal, authenticate, or configure a Vault server. Those security-sensitive actions remain separate future configuration intent.
- kind remains unusable in an Ubuntu plan until both verified artifact handling and an appropriate Linux container-engine product decision exist.
- Provider keys, Snap publishers, channels, architectures, and package indexes can change and must fail closed when reviewed evidence no longer matches.
- Real installation validation belongs only in a disposable Ubuntu 24.04 environment after Phase 4 orchestration exists.
- The ignored incomplete `.tmp/go-toolchain` cache remains local; use the complete Go 1.26.5 extraction via `COWEBS_GO_EXE` for validation.
- Publication remains source-only with no tag, release asset, or deployment.
